let quoteSample = "Beware of bugs in the above code; I have only proved it correct, not tried it.";
let vowelRegex = /[aeiou]/gi; // Changed this line, looks for any vowel, right? Doesn't *require* a character in the regex before the brackets, and therefore a dot? 
let result = quoteSample.match(vowelRegex); // Changed this line